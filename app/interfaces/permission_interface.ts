namespace IPermission {
  export enum Resources {
    USERS = 'users',
  }

  export enum Actions {
    LIST = 'list',
    READ = 'read',
    CREATE = 'create',
    UPDATE = 'update',
    DELETE = 'delete',
  }
}

export default IPermission
